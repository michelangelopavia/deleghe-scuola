[phases.setup]
nixPkgs = ["nodejs_20", "python3", "buildPackages.gcc"]

[phases.install]
cmds = ["npm install", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "node server/index.js"

[variables]
NODE_ENV = "production"
